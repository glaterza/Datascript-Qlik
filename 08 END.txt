/******Fin del Script******/

LEFT JOIN(TEMPLATE)
LOAD
	NOW() AS TEMPLATE.FIN
AUTOGENERATE 1;

LEFT JOIN(TEMPLATE)
LOAD
	TIME(TEMPLATE.FIN - TEMPLATE.INICIO, 'hh:mm:ss') AS TEMPLATE.DURACION
RESIDENT TEMPLATE;